# Ensure this file is saved in the CareSphere/backend directory

# Replace with the Gmail address you are sending the verification email FROM
EMAIL_USER=caresphereadmin@gmail.com

# Replace with the 16-character Google App Password (NOT your normal Gmail password!)
# To get an App Password: 
# 1. Manage Google Account -> Security -> 2-Step Verification (Must be ON) -> App Passwords
# 2. Select "Other (Custom name)" -> Type "CareSphere" -> Click Generate
EMAIL_PASS=ahyd jamt augp fdrm
